# Makefile.conf

# 1. For Compilation: Tell the C++ compiler to understand OpenMP pragmas.
CXXFLAGS += -fopenmp

# 2. For Linking: Add the OpenMP flag to the variables that are ACTUALLY
#    used in the final link command for the 'yosys' executable.
LINKFLAGS += -fopenmp
EXE_LINKFLAGS += -fopenmp
